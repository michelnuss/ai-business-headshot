import SwiftUI

enum StudioPalette {
    static let canvas = Color(red: 0.96, green: 0.95, blue: 0.93)
    static let card = Color.white
    static let accent = Color.accentColor
    static let ink = Color(red: 0.13, green: 0.14, blue: 0.16)
}
